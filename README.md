# Habits Tracker — Трекер атомных привычек

Бэкенд SPA-приложения для формирования полезных привычек по книге Джеймса Клира «Атомные привычки».

## Основной функционал

- Регистрация и авторизация пользователей по email (JWT)
- CRUD операций с привычками с учётом прав доступа:
  - Пользователь имеет полный доступ только к своим привычкам
  - Просмотр списка публичных привычек (только чтение)
- Пагинация списка привычек (по 5 элементов на страницу)
- Валидация данных привычек согласно требованиям книги (время выполнения ≤ 120 сек, периодичность 1–7 дней и др.)
- Асинхронные напоминания о выполнении привычек в Telegram (через Celery)
- Настройка CORS для подключения фронтенда
- Документация API через Swagger и ReDoc (drf-yasg)
- Покрытие тестами >80%
- Полная контейнеризация через Docker Compose (PostgreSQL, Redis, Celery worker, Celery beat)

## Технологии

- **Python 3.12**
- **Django 5** + **Django REST Framework**
- **JWT-авторизация** (djangorestframework-simplejwt)
- **PostgreSQL** — основная БД
- **Redis** + **Celery** — асинхронные задачи и напоминания
- **Telegram API** — отправка напоминаний (через requests)
- **Poetry** — управление зависимостями
- **Docker Compose** — запуск всех сервисов одной командой
- **drf-yasg** — документация API
- **django-cors-headers** — CORS

## Структура проекта

```
Habbit_tracker/
├── config/                 # Настройки Django
├── users/                  # Пользователи (кастомная модель без username)
├── habits/                 # Основное приложение: модели, views, serializers, задачи Celery
├── .env.example            # Шаблон переменных окружения
├── .env                    # Твои переменные (не в git!)
├── Dockerfile
├── docker-compose.yaml
├── pyproject.toml          # Poetry
├── manage.py
├── README.md
└── ...
```

## Запуск проекта

### Через Docker Compose (рекомендуемый способ)

1. Скопируй шаблон переменных:
   ```bash
   cp .env.example .env
   ```

2. Заполни `.env`:
   - `SECRET_KEY` — сгенерируй сильный ключ
   - `TELEGRAM_BOT_TOKEN` — токен от @BotFather
   - Остальные значения для PostgreSQL, Redis, CELERY

3. Запусти одной командой:
   ```bash
   docker compose up --build
   ```

4. Проверь:
   - API: http://localhost:8000
   - Swagger: http://localhost:8000/swagger/
   - ReDoc: http://localhost:8000/redoc/
   - Админка: http://localhost:8000/admin/

Создай суперпользователя:
```bash
docker exec -it habits_backend poetry run python manage.py createsuperuser
```

### Локальный запуск (без Docker)

```bash
poetry install
poetry run python manage.py migrate
poetry run celery -A config worker -l info
poetry run celery -A config beat -l info
poetry run python manage.py runserver
```

## Как работает Telegram-интеграция

- Пользователь заполняет `telegram_chat_id` в профиле (в админке или через будущий API).
- Celery каждую минуту проверяет привычки, время которых совпадает с текущим.
- При совпадении отправляет напоминание через Telegram HTTP API.
- Учитывается периодичность (не чаще, чем указано).

## Тесты

```bash
poetry run python manage.py test
```

Покрытие >80%: проверяются регистрация, права доступа, CRUD привычек, пагинация.

## Деплой на сервер (Yandex Cloud)

Проект настроен для автоматического деплоя через GitHub Actions.

### На сервере:
- Ubuntu 22.04
- Docker + Docker Compose
- SSH-доступ по ключу
- Открыты порты 80/443 (или 8000 для теста)

### Автоматический деплой:
При push в `main`:
1. Запускаются тесты
2. При успехе — код деплоится на сервер
3. Docker Compose перезапускается

Приложение доступно по IP сервера.

Безопасность:
- SSH только по ключу
- Ненужные порты закрыты
- Секреты в GitHub Secrets
- 
## Автор

MrchknRV — 2025

Проект выполнен в рамках курсовой работы.  
Готов к деплою и дальнейшему развитию (кнопки в Telegram, статистика, фронтенд).

**Спасибо за внимание!** 🚀